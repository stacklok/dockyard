# Stripe Agent Toolkit MCP Server Configuration
# Stripe API tools for payments, subscriptions, and customers
# Package: https://www.npmjs.com/package/@stripe/agent-toolkit
# Repository: https://github.com/stripe/agent-toolkit
# Will build as: ghcr.io/stacklok/dockyard/npx/stripe-agent-toolkit:0.9.0

metadata:
  name: stripe-agent-toolkit
  description: "MCP server providing Stripe API tools for managing payments, subscriptions, products, and customers"
  protocol: npx

spec:
  package: "@stripe/agent-toolkit"
  version: "0.9.0"

provenance:
  repository_uri: "https://github.com/stripe/agent-toolkit"
  repository_ref: "refs/heads/main"
