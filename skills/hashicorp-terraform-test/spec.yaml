# HashiCorp hashicorp-terraform-test Skill
# Source: https://github.com/hashicorp/agent-skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/hashicorp-terraform-test:0.1.0

metadata:
  name: hashicorp-terraform-test
  description: Comprehensive guide for writing and running Terraform tests. Use when creating test files
    (.tftest.hcl), writing test scenarios with run blocks, validating infrastructure behavior with assertions,
    mocking providers and data sources, testing module outputs and resource configurations, or troubleshooting
    Terraform test syntax and execution.

spec:
  repository: "https://github.com/hashicorp/agent-skills"
  ref: "326846817128fd1d052d25fbfded490ce2c5886e"  # main as of 2026-08-10
  path: "plugins/terraform/skills/terraform-test"
  version: "0.2.1"

provenance:
  repository_uri: "https://github.com/hashicorp/agent-skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "hashicorp/agent-skills is licensed MPL-2.0 at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
    - rule_id: ATR_2026_00013
      reason: "FP: matched example IP addresses or `internal/` package paths in Terraform configuration/code examples (e.g. SKILL.md:396, references/EXAMPLES.md:19) — standard placeholder addressing or Go module layout, not exfiltration to an internal network."
    - rule_id: ATR_2026_00066
      reason: "FP: matched Terraform HCL string-interpolation syntax (`${...}`) or GitHub Actions `secrets.*` references in documented configuration/CI examples (e.g. references/CI_CD.md:43, references/CI_CD.md:44) — standard HCL/CI syntax, not injected secrets."
    - rule_id: ATR_2026_00083
      reason: "FP: matched \"override\" describing Terraform variable/precedence override semantics in documentation prose (e.g. SKILL.md:73)."
    - rule_id: ATR_2026_00084
      reason: "FP: matched \"override\" describing Terraform variable/precedence override semantics in documentation prose (e.g. SKILL.md:44)."
    - rule_id: ATR_2026_00140
      reason: "FP: matched the word \"reverse\"/\"opposite\" in comparative documentation prose (e.g. SKILL.md:358, SKILL.md:402)."
    - rule_id: PG_PII_CREDENTIAL_HARVESTING
      reason: "FP: matched documentation describing the user's own cloud credential storage in Terraform Cloud varsets or CI secrets (e.g. references/CI_CD.md:80) — configuration guidance, not credential harvesting."
