# Firebase firebase-firestore Skill
# Cloud Firestore database setup, queries, and security rules (Standard + Enterprise editions).
# Source: https://github.com/firebase/agent-skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/firebase-firestore:0.1.0

metadata:
  name: firebase-firestore
  description: "Cloud Firestore database setup, queries, security rules, and data modeling — auto-detects Standard vs Enterprise Native Mode editions; replaces the previously separate firebase-firestore-standard and firebase-firestore-enterprise-native-mode skills, consolidated upstream"

spec:
  repository: "https://github.com/firebase/agent-skills"
  ref: "7288f7cf2a3ba9dfdd4ddd2b40c5ec9c8f2409b1"
  path: "skills/firebase-firestore"
  version: "0.3.0"

provenance:
  repository_uri: "https://github.com/firebase/agent-skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "firebase/agent-skills is licensed Apache-2.0 at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
    - rule_id: ATR_2026_00013
      reason: |
        False positive — matches `http://localhost:4000` in
        references/standard/provisioning.md, where the skill documents the
        standard Firebase Local Emulator Suite UI URL
        (`http://localhost:4000/firestore`) for local development. This is
        a documented Firebase developer URL that runs on the user's own
        machine, not an outbound network call. Verified at digest
        9067c1e3d3afbb212235356702db6ab4b82ec88a.
    - rule_id: BEHAVIOR_BASH_TAINT_FLOW
      reason: "FP: matched bash/curl code examples in Firestore API documentation — documentation, not agent-executable scripts."
    # Firebase SDK documentation skill — ATR pattern matches are all on
    # Firestore security rules syntax, API examples, and SDK usage docs.
    - category: POLICY_VIOLATION
      reason: "FP: firebase-firestore is a Firebase SDK documentation skill. All ATR pattern matches are on Firestore security rules syntax, API code examples, and SDK usage documentation — not agent instructions."
