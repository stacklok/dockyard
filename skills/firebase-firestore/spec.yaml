# Firebase firebase-firestore Skill
# Cloud Firestore database setup, queries, and security rules (Standard + Enterprise editions).
# Source: https://github.com/firebase/agent-skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/firebase-firestore:0.1.0

metadata:
  name: firebase-firestore
  description: "Cloud Firestore database setup, queries, security rules, and data modeling — auto-detects Standard vs Enterprise Native Mode editions; replaces the previously separate firebase-firestore-standard and firebase-firestore-enterprise-native-mode skills, consolidated upstream"

spec:
  repository: "https://github.com/firebase/agent-skills"
  ref: "9067c1e3d3afbb212235356702db6ab4b82ec88a"
  path: "skills/firebase-firestore"
  version: "0.1.0"

provenance:
  repository_uri: "https://github.com/firebase/agent-skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "firebase/agent-skills is licensed Apache-2.0 at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
