# MongoDB mongodb-schema-design Skill
# Design effective MongoDB schemas using document modeling patterns.
# Source: https://github.com/mongodb/agent-skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/mongodb-schema-design:0.1.0

metadata:
  name: mongodb-schema-design
  description: "Design effective MongoDB schemas — document modeling patterns (embedding, referencing, bucketing, polymorphism, extended reference), cardinality trade-offs, and index strategy"

spec:
  repository: "https://github.com/mongodb/agent-skills"
  ref: "47cc46148f53145eb9b880d2bf1aa89bc9097818"
  path: "skills/mongodb-schema-design"
  version: "0.2.1"

provenance:
  repository_uri: "https://github.com/mongodb/agent-skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "mongodb/agent-skills is licensed Apache-2.0 at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
    - rule_id: BEHAVIOR_BASH_TAINT_FLOW
      reason: "FP: matched bash/mongo shell code examples in schema design documentation — documentation, not agent-executable scripts."
    # MongoDB documentation skill — ATR pattern matches are all on MongoDB
    # aggregation pipeline syntax, schema examples, and shell commands.
    - category: policy_violation
      reason: "FP: mongodb-schema-design is a MongoDB documentation skill. All ATR pattern matches are on MongoDB aggregation pipeline syntax ($$ROOT, $lookup), schema design examples, and shell command documentation — not agent instructions."
    - category: prompt_injection
      reason: "FP: same as policy_violation above — all pattern matches are on documentation, code examples, or attack pattern descriptions for detection purposes, not agent instructions."
    - rule_id: PG_PII_SSN_HARVESTING
      reason: "FP: matched 'require choosin' in schema design documentation about data modeling decisions — not PII harvesting."
