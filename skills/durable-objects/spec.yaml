# Cloudflare durable-objects Skill
# Create and review Cloudflare Durable Objects.
# Source: https://github.com/cloudflare/skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/durable-objects:0.1.0

metadata:
  name: durable-objects
  description: "Create and review Cloudflare Durable Objects — stateful coordination (chat rooms, multiplayer, booking), RPC methods, SQLite storage, alarms, WebSockets, wrangler config, Vitest testing"

spec:
  repository: "https://github.com/cloudflare/skills"
  ref: "f96bff754e428838818017f75817f0f9428acd48"  # main as of 2026-04-16
  path: "skills/durable-objects"
  version: "0.2.2"

provenance:
  repository_uri: "https://github.com/cloudflare/skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "cloudflare/skills is licensed Apache-2.0 at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
    - rule_id: ATR_2026_00063
      reason: "FP: cisco-ai-skill-scanner matched 'UPLOAD' (references/workers.md:40,88) - an R2 bucket binding name ('UPLOADS') in a wrangler.toml config example - and 'sendMessage'/'SendMessage' (references/workers.md:149,163,168,170,180) - a Durable Object RPC method name and a Zod schema name in example chat-room code. Ordinary example code, no executable threat."
    - rule_id: ATR_2026_00012
      reason: "FP: cisco-ai-skill-scanner matched 'execute' in `wrangler d1 execute DB --command \"SELECT * FROM _cf_DO\"` (references/workers.md:345) - the standard, documented Wrangler CLI command for querying a D1 database, not arbitrary code execution."
