# Neon Object Storage Skill
# Source: https://github.com/neondatabase/agent-skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/neon-object-storage:0.1.0

metadata:
  name: neon-object-storage
  description: >-
    S3-compatible object storage that branches with your Neon project, so files
    and the database stay in sync across every branch. Use when a user wants
    object storage, a bucket, blob/file storage, or somewhere to put uploads,
    images, documents, avatars, or user-generated files for their app or agent —
    especially when they already use (or are setting up) Lakebase Postgres and
    don't want to add a separate storage provider like AWS S3, Cloudflare R2, or
    Supabase Storage. Triggers include "object storage", "bucket", "blob
    storage", "file storage", "store uploads/images/files", "S3-compatible
    storage", "presigned URL", "where do I put files", "storage logs",
    "bucket logs", "CDN in front of object storage", "Neon Object Storage",
    "Neon Storage", and "storage that branches with my database".

spec:
  repository: "https://github.com/neondatabase/agent-skills"
  ref: "b5beae8767c18ee7b9dc1ae1eb6c93c7cb1a96c4"  # main as of 2026-09-18
  path: "skills/neon-object-storage"
  version: "0.1.0"

provenance:
  repository_uri: "https://github.com/neondatabase/agent-skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "neondatabase/agent-skills is licensed Apache-2.0 at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
