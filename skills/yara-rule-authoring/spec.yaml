# Trail of Bits yara-rule-authoring Skill
# Guides authoring of high-quality YARA-X detection rules.
# Source: https://github.com/trailofbits/skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/yara-rule-authoring:0.1.0

metadata:
  name: yara-rule-authoring
  description: "Author high-quality YARA-X detection rules for malware identification — covers naming conventions, string selection, performance optimization, migration from legacy YARA, and false-positive reduction"

spec:
  repository: "https://github.com/trailofbits/skills"
  ref: "d1f1575cff97816e5cc08af66cd2506099c681d3"
  path: "plugins/yara-authoring/skills/yara-rule-authoring"
  version: "0.2.3"

provenance:
  repository_uri: "https://github.com/trailofbits/skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "trailofbits/skills is licensed CC-BY-SA-4.0 at the repository root; upstream does not embed a license field in per-skill SKILL.md frontmatter."
    # Security analysis skill — reference files intentionally describe
    # YARA detection patterns, testing approaches, and deployment workflows.
    - category: policy_violation
      reason: "FP: yara-rule-authoring is a security analysis skill whose reference files intentionally describe YARA detection patterns, testing commands, and deployment procedures. All ATR pattern matches are documentation of detection rule authoring, not agent instructions."
    - category: prompt_injection
      reason: "FP: same as policy_violation above — all pattern matches are on documentation, code examples, or attack pattern descriptions for detection purposes, not agent instructions."
    - rule_id: PG_PII_SSN_HARVESTING
      reason: "FP: matched 'Gather samples' in YARA testing documentation — refers to collecting malware samples for testing detection rules, not harvesting PII."
