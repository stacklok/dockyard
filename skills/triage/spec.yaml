# Matt Pocock triage Skill
# Triage issues through a state machine of triage roles.
# Source: https://github.com/mattpocock/skills
# Will publish as: ghcr.io/stacklok/dockyard/skills/triage:0.1.0

metadata:
  name: triage
  description: "Move issues and external PRs through a state machine of triage roles — categorise, verify, grill if needed, and write agent-ready briefs."

spec:
  repository: "https://github.com/mattpocock/skills"
  ref: "068b6e0c62393147daf03530149cdce209c93da8"  # main as of 2026-08-06
  path: "skills/engineering/triage"
  version: "0.4.2"

provenance:
  repository_uri: "https://github.com/mattpocock/skills"
  repository_ref: "refs/heads/main"

security:
  allowed_issues:
    - rule_id: MANIFEST_MISSING_LICENSE
      reason: "mattpocock/skills is licensed MIT at the repository root; upstream does not embed an SPDX license identifier in per-skill SKILL.md frontmatter."
    - rule_id: ATR_2026_00051
      reason: "FP: 'all files' matches OUT-OF-SCOPE.md's instruction to read all files in the project-local `.out-of-scope/` knowledge-base directory (OUT-OF-SCOPE.md:72), a scoped and bounded read, not an unbounded filesystem scan."
